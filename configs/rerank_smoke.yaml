run_name: rerank_smoke

input:
  event_log_base_dir: data/interim/event_logs
  event_log_run_name: mind_smoke_event_log
  ranker_base_dir: data/processed/ranker
  ranker_run_name: ranker_smoke

reranking:
  freshness_weight: 2.5
  prediction_guard_margin: 0.35
  topic_repeat_penalty: 0.20
  creator_repeat_penalty: 0.15

output:
  base_dir: data/processed/reranked_feed

artifacts:
  base_dir: artifacts/runs
