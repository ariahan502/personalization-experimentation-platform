run_name: candidates_smoke

input:
  event_log_base_dir: data/interim/event_logs
  event_log_run_name: mind_smoke_event_log

retrieval:
  candidate_count: 4
  sources:
    - name: affinity
      candidate_count: 3
      priority: 1
    - name: content
      candidate_count: 3
      priority: 2
    - name: trending
      candidate_count: 3
      priority: 3

output:
  base_dir: data/processed/candidates

artifacts:
  base_dir: artifacts/runs
